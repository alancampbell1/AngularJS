This project demonstrates how a Single Page Application works and routing in AngularJS.
The goal is to create a Single Page Application that does not need a page refresh when changing pages and to show different corresponding pieces of data.

To install a local server go:
npm install -g http-server

To run go:
http-server -o

